interface:
  display_name: "Watchman GPU Operations"
  short_description: "Operate and benchmark inference GPUs safely"
  default_prompt: "Use $watchman to inspect this GPU node and return privacy-safe evidence with the correct exit-code interpretation."
